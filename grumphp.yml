parameters:
  bin_dir: ./vendor/bin
  git_dir: .
  tasks:
    phpcs:
      standard: PSR12
      exclude:
        - database
        - storage
        - bootstrap
    phpcpd:
      exclude:
        - database
        - storage
        - bootstrap
    phpstan:
      configuration: ./phpstan.neon.dist
    composer: ~
    phpunit: ~
#  ascii:
#    failed: grumphp-grumpy.txt
#    succeeded: grumphp-happy.txt
